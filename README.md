Transfer Control Protocol(TCP) skeleton
This is a preliminary version of a HTTP server, eventually HTTP parsing and response handling will be included, but this is the core of the server, the TCP transport.
It will eventually scale up to multi-client architecture with "select()", contain a server class for optimisation, and event-driven architecture.